[project]
name = "oracle-aidev-template"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "oracledb>=3.0",
    "httpx>=0.28",
    "pydantic>=2.0"
]

[dependency-groups]
dev = []

[tool.uv]
